puts fn (^)U8 -> I32

add (a, b) = a + b

main fn Unit -> Unit = (
	v0 = 64
	v1 = add (v0, 3)

	puts v1+3

	looking Bool =
		running
		or falling
		or (eating or drinking)

	puts ('one, "hello, world.\n\0", 15)
)

Person = (
	name ()u8,
	age  u32,
)

Token type =
	| binop
	| unop
	| number

Result type = ok | err ()u8

