$Vec2 ..
@add ..

@puts (ptr) -> i32

$math/Vec2 (i32, i32)
@math/add $math/Vec2 -> i32

@main* unit -> unit
    var.0 $Vec2
    var.0 [0] = i32 10
    var.0 [4] = i32 20
    var.1 = call @add var.0
    call @puts "hello, world.\n\0"
    ret

## math.cosm ##
$Vec2* (x i32, y i32)

@add* $Vec2 -> i32
    var.0 i32 = arg.1 [0]
    var.1 i32 = arg.1 [4]
    var.2 i32 = add var.0 var.1
    ret var.2
