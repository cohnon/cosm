@write foreign fn (i32, *i8, word) -> i32

# types:
#   1
#   fn tup 2 i32 ptr i8 i32
# foreigns:
#   mod(str) name(str) type(0)
# globals:
#   name(str) type(0)
# locals:
#   type(0)
# strings:
#   2
#   4 5
#   libcwrite

# @main global fn unit -> unit
#     %0 = imm i32 64
#     %1 = call @add (%0, 3)
# 
#     call @libc/write (1, ref %1, 1)
#     call @libc/write (1, ref @hello_str, 15)
# 
#     ret
# end
# 
# @add fn (i32, i32) -> i32
#     %2 = add %0 %1
#     ret %2
# end
# 
# @add fn i32 -> i32
#     ret %1
# end
#
# @hello_str foreign [15 x mutable i8] "hello, world.\n\0"
